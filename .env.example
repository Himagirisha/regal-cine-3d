# Backend API base URL
# Local development: default is http://localhost:8080 (no change needed)
# Production (Render / Railway): set this to your deployed backend URL
VITE_API_URL=https://your-backend.onrender.com
